Extract all text from this image, following these rules:

1. Extract completely — do not miss any visible text, including titles, body text, labels, buttons, footers, etc.
2. Preserve original formatting — tables keep row/column structure, lists keep indentation and numbering
3. Output in reading order — top to bottom, left to right
4. Do not summarize or paraphrase — output exactly what you see
5. For printed text, pay attention to accurate punctuation and capitalization

If the image contains a table, reproduce it in Markdown table format.
